<h1>What are you about to reveal?</h1>


<% if @users.length > 0 %>
  <ul>
    <% @users.each do |user| %>
      <li class="user"> <%= link_to user.username, user_path(user.id) %> </li>
    <% end %>
  </ul>
<% else %>
  <p> Seem like no one likes to share secrets... </p>
<% end %>
